/**
 * RateLimiterService (Single Responsibility Principle)
 * Client-side request rate-limiting logic (Unlimited access).
 */
export class RateLimiterService {
  constructor() {}

  check() {
    // Unlimited access: No rate limiting enforced
  }
}
